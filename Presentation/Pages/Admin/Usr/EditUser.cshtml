@page
@using Application.Common.Models;
@model Presentation.Pages.Admin.Usr.EditUserModel
@{
    ViewBag.Title = "Edit User";
}
<!-- Main content -->
<section class="content">
    <div class="card card-primary">
        <div class="card-header">
            <h3 class="card-title">General</h3>

            <div class="card-tools">
                <button type="button" class="btn btn-tool" data-card-widget="collapse" title="Collapse">
                    <i class="fas fa-minus"></i>
                </button>
            </div>
        </div>
        <div class="card-body">
            <form method="post" asp-page-handler="Update">
                <input type="hidden" asp-for="UserP.Id" />
                <div class="form-group">
                    <label for="inputDescription">Name</label>
                    <input type="text" asp-for="UserP.Name" class="form-control">
                </div>
                <div class="form-group">
                    <label for="inputName">Surname</label>
                    <input type="text" asp-for="UserP.Surname" class="form-control">
                </div>
                <div class="form-group">
                    <label for="inputName">Email</label>
                    <input type="email" asp-for="UserP.Email" class="form-control">
                </div>
                <div class="form-group">
                    <label for="inputName">Role ID</label>
                    <select id="inputStatus" asp-for="UserP.RoleId" class="form-control custom-select">
                        <option selected disabled>Select one</option>
                        @foreach (RoleDto role in Model.Roles)
                        {
                            <option value="@role.Id">
                                <div>@role.Id - @role.Title</div>
                            </option>
                        }
                    </select>
                </div>
                <div class="row">
                    <div class="col-12">
                        <a asp-page="/Admin/Lists/User" class="btn btn-secondary">Cancel</a>
                        <input type="submit" value="Save Changes" class="btn btn-success float-right">
                    </div>
                </div>
            </form>
            <!-- /.card-body -->
        </div>
    </div>
</section>

