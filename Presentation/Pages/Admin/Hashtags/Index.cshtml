@page
@using Domain.Entities;
@using Microsoft.AspNetCore.Identity;
@inject UserManager<User> UserManager;
@model Presentation.Pages.Admin.Lists.HashtagsModel
@{
    ViewBag.Title = "Hastags";
    var currentUser = await UserManager.GetUserAsync(User);
    bool userIsAdmin = await UserManager.IsInRoleAsync(currentUser, "Admin");
}
@using Application.Common.Models;

<section class="content">
    <div class="card">
        <div class="card-header">
            <button type="button" onclick="location.href='@Url.Page("/Admin/Hashtags/Add")'" class="btn btn-primary">Add Hashtag</button>

            <div class="card-tools">
                <button type="button" class="btn " data-card-widget="collapse" title="Collapse">
                    <i class="fas fa-minus"></i>
                </button>
                <button type="button" class="btn " data-card-widget="remove" title="Remove">
                    <i class="fas fa-times"></i>
                </button>
            </div>
        </div>
        <div class="card-body p-0">
            <table class="table table-striped projects">
                <thead>
                    <tr>
                        @foreach (var property in typeof(HashtagDto).GetProperties())
                        {
                            if (property.Name != "Id")
                            {
                                <th>@property.Name</th>
                            }
                        }
                        <th style="width: 20%">
                        </th>
                    </tr>
                </thead>
                <tbody>
                    @foreach (var hashtag in Model.Hashtags)
                    {
                        <tr>
                            @foreach (var property in typeof(HashtagDto).GetProperties())
                            {
                                if (property.Name != "Id")
                                {
                                    <td>@property.GetValue(hashtag)</td>
                                }
                            }
                            <td class="project-actions text-right">
                                <form method="post">
                                    <a class="btn btn-primary btn-sm"
                                       asp-page="/Admin/Hashtags/Details"
                                       asp-route-Id="@hashtag.Id">
                                        <i class="fas fa-folder">
                                        </i>
                                        View
                                    </a>
                                    <a class="btn btn-info btn-sm"
                                       asp-page="/Admin/Hashtags/Edit"
                                       asp-route-Id="@hashtag.Id">
                                        <i class="fas fa-pencil-alt">
                                        </i>
                                        Edit
                                    </a>

                                    @if (userIsAdmin)
                                    {

                                        <button class="btn btn-danger btn-sm"
                                                type="submit"
                                                asp-page-handler="Delete"
                                                asp-route-id="@hashtag.Id">
                                            <i class="fas fa-trash">
                                            </i>
                                            Delete
                                        </button>
                                    }
                                </form>
                            </td>
                        </tr>

                    }
                </tbody>
            </table>
        </div>
    </div>
</section>
